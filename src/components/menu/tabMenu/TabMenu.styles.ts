import styled from "styled-components";


const TabMenuList = styled.ul`
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    gap: 1.5rem;
`

const S = {
    TabMenuList,
} 


export default S